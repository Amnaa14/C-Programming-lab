#include <stdio.h>
int main()
{
int x, y;
printf("Enter two integers (x and y):\n");
scanf("%d %d",&x,&y);
printf("(x > 5) && (y < 10): %d\n", (x > 5) && (y < 10));
printf("(x == 10) || (y == 5): %d\n", (x == 10) || (y == 5));
printf("!(x == y): %d\n", !(x == y));
return 0;
}
